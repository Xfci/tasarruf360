import { Text, View } from 'react-native'
import { styles } from '../../style';

const Profile = ({ }) => {
    return (
        <View>
            <Text>
                PROFİLE
            </Text>
        </View>
    )
}

export default Profile;